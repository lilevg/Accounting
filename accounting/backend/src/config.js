const config = {
    dbName: "course_work_accounting",
    dbUser: "root",
    dbPassword: "root",
    dbHost: "localhost",
    dbPort: 3306,
    jwtSecret: "super_secret",
    applicationPort: 5003
}

module.exports = config;